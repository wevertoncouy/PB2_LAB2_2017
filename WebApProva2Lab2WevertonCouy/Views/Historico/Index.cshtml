@model IEnumerable<WebApProva2Lab2WevertonCouy.Models.Historico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Detalhes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataHora)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Detalhes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataHora)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HistoricoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HistoricoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HistoricoId })
        </td>
    </tr>
}

</table>
